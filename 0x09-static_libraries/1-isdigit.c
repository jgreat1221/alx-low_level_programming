#include "main.h"
/**
 * _isdigit - function that check for a digit (0 through 9)
 * @c: int to be checked
 * Return: Always 0 (Successful)
 */
int _isdigit(int c)

{
	return (c >= '0' && c <= '9');
}

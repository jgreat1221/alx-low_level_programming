#include "main.h"
/**
 * mul - function that checks for a digt (0 through)
 * @a: a - variable
 * @b: b - variable
 * Return: Always 0 (Successful)
 */
int mul(int a, int b)

{
	int mul;

	mul  = a * b;
	
	return (mul);
}
